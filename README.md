### To run the application please open the index.html in your browser. 
### Alternatively try to run the node.js static `http-server` on project path (you need to install node and npm package for that).
